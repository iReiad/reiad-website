/* No `scripts`. The form's own behaviour was `/contact-form.js`
   and is `components/contact-form.tsx`, which is a component of
   the page rather than a module loaded beside it. */
import { siteLayout } from "../../../components/page";

export default siteLayout({ current: "contact" });
