# The map

Generated by `scripts/build-map.ts` from the files themselves, so it is
never out of date: the generated stage of `check-all.ts` fails when it
drifts. **Read this once at the start of a task**, after `CLAUDE.md`,
and open a file only to change it. Every line is the first sentence of
the file's own header.

## How the site is put together

- **Two Workers.** `worker.js` is the front door: it serves `aab/` as
  static assets, answers the API under `functions/`, and forwards every
  address in `NEXT_ROUTES` to the Next Worker, which is `next/` built
  by OpenNext. `wrangler.toml` is the first Worker's config.
- **Where the data is.** Articles, lessons and settings are in D1
  (Cloudflare's SQLite), written through the Studio at `/studio` and
  read by the routes. Accounts, progress, saved things and the research
  tables are in Supabase, behind row-level security, read by the browser
  with the reader's own token. Photos and nightly backups are in R2.
- **What the browser runs.** `aab/src/*.ts` are the served modules,
  compiled to `aab/*.js` by `scripts/build-modules.ts`; a Next route
  loads one through `<SiteScripts>`. `app/src/` is the React Studio,
  built to `aab/studio/`. React components under `next/components/`
  are the rest of the interface.
- **What three runtimes share.** `shared/` is imported by the Worker,
  the browser (compiled) and Next (as `@reiad/shared`, a copied package:
  refresh it after editing, see CLAUDE.md).
- **The look.** `next/styles/site.css` is the design system in cascade
  layers (`tokens`, `base`, `shell`, `components`, `deck`, `article`,
  `lesson`, one per section, `tw` last); `tailwind.css` maps the tokens
  to utilities; `aab/fallback.css` is the built copy for the two pages
  that are files. Colours, type, corners, spacing and motion are tokens
  on `:root` at the top of `site.css`.
- **The menu.** `shared/nav.ts` is the one table the rail, the top bar,
  the footer and `/api/site` read.
- **Verifying.** `node scripts/check-all.ts --changed` for what the diff
  could have broken, `node scripts/check-all.ts` before a pull request,
  and the browser suites in the Tests table for the areas touched.

## Where to change what

| to add or change | edit |
| --- | --- |
| a menu entry, a school, a tool, a section's colour | `shared/nav.ts` (and `shared/content.ts` for a count or a page) |
| a lesson's prose | the Studio at `/studio/?lessons`, or the seed under `scripts/money/`, `scripts/english/` |
| a ladder (stages, lessons, ids) | `shared/curricula/<school>.ts`, then `scripts/build-modules.ts` |
| an article block class | `@layer article` in `site.css`, `KEEP_CLASSES` in `aab/editor.js`, `ALLOWED_CLASSES` in `functions/_lib/sanitise.ts` |
| a storage key | a row in `shared/storage.ts`, and `KEYS` in `aab/src/sync.ts` if it syncs |
| a colour, a face, a corner, a spacing step | the tokens at the top of `next/styles/site.css`, then `scripts/build-fallback.ts` |
| a served module | `aab/src/<name>.ts`, then `scripts/build-modules.ts`, then bump `VERSION` in `aab/sw.js` and `scripts/check-sw.ts --update` |
| an API endpoint | `functions/api/`, gated by `requireAdmin`/`isAdmin` or named public in `scripts/check-admin.ts` |
| a route | a directory under `next/app/`, `run_worker_first` in `wrangler.toml`, `NEXT_ROUTES` in `worker.js` |
| a table a reader owns | a migration under `supabase/migrations/`, plus the copy and the erase in `aab/src/account-page.ts` |
| a check | `scripts/check-<thing>.ts`, listed in `scripts/check-all.ts` |
| the design | `DESIGN.md` says what it is today and what is still wrong; change it, then say so there |


## Routes

Every `page.tsx` under `next/app/`, by address.

| address | what it is |
| --- | --- |
| `/[section]/[slug]/[lesson]` | A lesson of one of the four schools, rendered from its row. (`next/app/[section]/[slug]/[lesson]/page.tsx`) |
| `/[section]/[slug]/arbeitsbuch` | /deutsch/<stufe>/arbeitsbuch: the German practice book. (`next/app/[section]/[slug]/arbeitsbuch/page.tsx`) |
| `/[section]/[slug]/workbook` | /english/<term>/workbook: the English practice book. (`next/app/[section]/[slug]/workbook/page.tsx`) |
| `/[section]/[slug]` | Two pages answer at `/<first>/<second>`, and this file is the one route Next can be given for them. (`next/app/[section]/[slug]/page.tsx`) |
| `/[section]/advanced` | /deutsch/advanced: the Netzwerk neu study planner. (`next/app/[section]/advanced/page.tsx`) |
| `/[section]/contents` | /money/contents, the money school's full index. (`next/app/[section]/contents/page.tsx`) |
| `/[section]` | Three kinds of index page share this address. (`next/app/[section]/(hub)/page.tsx`) |
| `/` | The front door. Every band is one LEAD and a set behind it, in the order somebody arriving asks: what is this, is it any good,... (`next/app/(home)/page.tsx`) |
| `/about` | /about Ported out of `aab/about.html` with archive/TRANSITION.md Stage 11.5, words unchanged. (`next/app/(site)/about/page.tsx`) |
| `/account` | /account: the one page about the reader rather than about the writing. (`next/app/(site)/account/page.tsx`) |
| `/admin` | /admin ADMIN.md is the plan and this is stage 1 of it: the route, the shell, the two sign-ins and Health. (`next/app/(site)/admin/(panel)/page.tsx`) |
| `/contact` | /contact Ported out of `aab/contact.html` with archive/TRANSITION.md Stage 11.5. (`next/app/(site)/contact/page.tsx`) |
| `/insights` | This route has no dynamic segment, so Next would render it once at build time and serve that for ever: a hub whose list of piec... (`next/app/insights/page.tsx`) |
| `/portfolio/dcf` | /portfolio/dcf Ported out of `aab/portfolio/dcf.html` with archive/TRANSITION.md Stage 11.3, words unchanged: this is one of th... (`next/app/(site)/portfolio/dcf/page.tsx`) |
| `/portfolio/dissertation` | /portfolio/dissertation Ported out of `aab/portfolio/dissertation.html` with archive/TRANSITION.md Stage 11.3, words unchanged:... (`next/app/(site)/portfolio/dissertation/page.tsx`) |
| `/portfolio/dsex` | /portfolio/dsex Ported out of `aab/portfolio/dsex.html` with archive/TRANSITION.md Stage 11.3, words unchanged: this is one of... (`next/app/(site)/portfolio/dsex/page.tsx`) |
| `/portfolio/frontier` | /portfolio/frontier Ported out of `aab/portfolio/frontier.html` with archive/TRANSITION.md Stage 11.3, words unchanged: this is... (`next/app/(site)/portfolio/frontier/page.tsx`) |
| `/portfolio/scorecard` | /portfolio/scorecard Ported out of `aab/portfolio/scorecard.html` with archive/TRANSITION.md Stage 11.3, words unchanged: this... (`next/app/(site)/portfolio/scorecard/page.tsx`) |
| `/portfolio/stress` | /portfolio/stress Ported out of `aab/portfolio/stress.html` with archive/TRANSITION.md Stage 11.3, words unchanged: this is one... (`next/app/(site)/portfolio/stress/page.tsx`) |
| `/portfolio/three-statement` | /portfolio/three-statement Ported out of `aab/portfolio/three-statement.html` with archive/TRANSITION.md Stage 11.3, words unch... (`next/app/(site)/portfolio/three-statement/page.tsx`) |
| `/portfolio` | /portfolio: the portfolio index. (`next/app/(site)/portfolio/(hub)/page.tsx`) |
| `/skills/courses/[programme]/[course]/[module]/[lesson]` | /skills/courses/<programme>/<course>/<module>/<lesson>.html: the lesson, its video, its files and the button that finishes it. (`next/app/(site)/skills/courses/[programme]/[course]/[module]/[lesson]/page.tsx`) |
| `/skills/courses/[programme]/[course]/[module]` | /skills/courses/<programme>/<course>/<module>: the module summary, which is where the last lesson of a module lands. (`next/app/(site)/skills/courses/[programme]/[course]/[module]/page.tsx`) |
| `/skills/courses/[programme]/[course]` | /skills/courses/<programme>/<course>: one course, and the deep link into the first lesson its reader has not ticked. (`next/app/(site)/skills/courses/[programme]/[course]/page.tsx`) |
| `/skills/courses/[programme]` | /skills/courses/<programme>: one certificate, its courses in order, and how much of each there is. (`next/app/(site)/skills/courses/[programme]/page.tsx`) |
| `/skills/courses` | /skills/courses: the shelf of programmes. (`next/app/(site)/skills/courses/page.tsx`) |
| `/skills` | /skills, the list of everything this site teaches. (`next/app/(site)/skills/(hub)/page.tsx`) |
| `/studio` | /studio The Studio's shell, ported out of the hand-written page Vite used to build it from, with archive/TRANSITION.md Stage 11.6. (`next/app/(site)/studio/page.tsx`) |
| `/tools/diet/expect` |  (`next/app/(site)/tools/diet/expect/page.tsx`) |
| `/tools/diet/foods` |  (`next/app/(site)/tools/diet/foods/page.tsx`) |
| `/tools/diet/glossary` | /tools/diet/glossary The words, in both languages. (`next/app/(site)/tools/diet/glossary/page.tsx`) |
| `/tools/diet/goal` |  (`next/app/(site)/tools/diet/goal/page.tsx`) |
| `/tools/diet/habits` |  (`next/app/(site)/tools/diet/habits/page.tsx`) |
| `/tools/diet/health` |  (`next/app/(site)/tools/diet/health/page.tsx`) |
| `/tools/diet/import` |  (`next/app/(site)/tools/diet/import/page.tsx`) |
| `/tools/diet/journal` |  (`next/app/(site)/tools/diet/journal/page.tsx`) |
| `/tools/diet/keto` |  (`next/app/(site)/tools/diet/keto/page.tsx`) |
| `/tools/diet/nutrition` |  (`next/app/(site)/tools/diet/nutrition/page.tsx`) |
| `/tools/diet/recipes` |  (`next/app/(site)/tools/diet/recipes/page.tsx`) |
| `/tools/diet/summary` |  (`next/app/(site)/tools/diet/summary/page.tsx`) |
| `/tools/diet/trend` |  (`next/app/(site)/tools/diet/trend/page.tsx`) |
| `/tools/diet/year` |  (`next/app/(site)/tools/diet/year/page.tsx`) |
| `/tools/diet/you` | /tools/diet/you The body page. (`next/app/(site)/tools/diet/you/page.tsx`) |
| `/tools/diet` | /tools/diet The diet tool's front door. (`next/app/(site)/tools/diet/page.tsx`) |
| `/tools/live` | /tools/live The live portfolio: a Trading 212 account, read through the site's own Worker at /api/broker and drawn by /tools/li... (`next/app/(site)/tools/live/page.tsx`) |
| `/tools/research/archive` | /tools/research/archive. (`next/app/(site)/tools/research/archive/page.tsx`) |
| `/tools/research/ask` | /tools/research/ask. RESEARCH.md is the plan; the room's own file is the reasoning. The frame takes the title, the colour and t... (`next/app/(site)/tools/research/ask/page.tsx`) |
| `/tools/research/atlas` | /tools/research/atlas. (`next/app/(site)/tools/research/atlas/page.tsx`) |
| `/tools/research/clip` | /tools/research/clip?u=: the bookmarklet's landing. (`next/app/(site)/tools/research/clip/page.tsx`) |
| `/tools/research/field` | /tools/research/field. (`next/app/(site)/tools/research/field/page.tsx`) |
| `/tools/research/find` | /tools/research/find. RESEARCH.md is the plan; the room's own file is the reasoning. The frame takes the title, the colour and... (`next/app/(site)/tools/research/find/page.tsx`) |
| `/tools/research/lab/run/[id]` | /tools/research/lab/run/<id>: one run, whole, so a draft's figure or table can point at it. (`next/app/(site)/tools/research/lab/run/[id]/page.tsx`) |
| `/tools/research/lab` | /tools/research/lab. RESEARCH.md is the plan; the room's own file is the reasoning. The frame takes the title, the colour and t... (`next/app/(site)/tools/research/lab/page.tsx`) |
| `/tools/research/library/[id]` | /tools/research/library/<id>: one source, by id. (`next/app/(site)/tools/research/library/[id]/page.tsx`) |
| `/tools/research/library` | /tools/research/library. (`next/app/(site)/tools/research/library/page.tsx`) |
| `/tools/research/methods/[slug]` | /tools/research/methods/<slug>: one lesson written here, in the article's own vocabulary, for a method no piece covers yet. (`next/app/(site)/tools/research/methods/[slug]/page.tsx`) |
| `/tools/research/methods` | /tools/research/methods. (`next/app/(site)/tools/research/methods/page.tsx`) |
| `/tools/research/notes/[id]` | /tools/research/notes/<id>: one note, by id. (`next/app/(site)/tools/research/notes/[id]/page.tsx`) |
| `/tools/research/notes` | /tools/research/notes. (`next/app/(site)/tools/research/notes/page.tsx`) |
| `/tools/research/plan/week` | /tools/research/plan/week: the weekly review. (`next/app/(site)/tools/research/plan/week/page.tsx`) |
| `/tools/research/plan` | /tools/research/plan. RESEARCH.md is the plan; the room's own file is the reasoning. The frame takes the title, the colour and... (`next/app/(site)/tools/research/plan/page.tsx`) |
| `/tools/research/questions` | /tools/research/questions. (`next/app/(site)/tools/research/questions/page.tsx`) |
| `/tools/research/read` | /tools/research/read: the queue, or the reader when `?source=<id>&file=<key>` names a source. (`next/app/(site)/tools/research/read/page.tsx`) |
| `/tools/research/review` | /tools/research/review. (`next/app/(site)/tools/research/review/page.tsx`) |
| `/tools/research/settings` | /tools/research/settings. (`next/app/(site)/tools/research/settings/page.tsx`) |
| `/tools/research/survey/[token]` | /tools/research/survey/<token>: a field room survey as a stranger sees it. (`next/app/(site)/tools/research/survey/[token]/page.tsx`) |
| `/tools/research/tools/[tool]` | /tools/research/tools/<slug>: one tool of the workshop, a page a reader can link to. (`next/app/(site)/tools/research/tools/[tool]/page.tsx`) |
| `/tools/research/tools` | /tools/research/tools. (`next/app/(site)/tools/research/tools/page.tsx`) |
| `/tools/research/write` | /tools/research/write. (`next/app/(site)/tools/research/write/page.tsx`) |
| `/tools/research` | /tools/research: the board, and the front door of the studio. (`next/app/(site)/tools/research/page.tsx`) |
| `/tools/routine/day` | /tools/routine/day Its own address rather than a tab, because a tab is not a place: it cannot be linked, bookmarked, or opened... (`next/app/(site)/tools/routine/day/page.tsx`) |
| `/tools/routine/print` | /tools/routine/print The week on paper, and the reason it exists is the last sentence of the build spec's section 8: "this is t... (`next/app/(site)/tools/routine/print/page.tsx`) |
| `/tools/routine/settings` | /tools/routine/settings Everything that shapes the routine: the list, the templates, and your own copy of all of it. (`next/app/(site)/tools/routine/settings/page.tsx`) |
| `/tools/routine/year` | /tools/routine/year Its own address rather than a tab, because a tab is not a place: it cannot be linked, bookmarked, or opened... (`next/app/(site)/tools/routine/year/page.tsx`) |
| `/tools/routine` | /tools/routine A day of somebody's own: mark what you did, write one good thing, and keep it. (`next/app/(site)/tools/routine/page.tsx`) |
| `/tools/stock` | /tools/stock Ported out of `aab/tools/stock.html` with archive/TRANSITION.md Stage 11.4, words unchanged. (`next/app/(site)/tools/stock/page.tsx`) |
| `/tools` | /tools Ported out of `aab/tools/index.html` with archive/TRANSITION.md Stage 11.4, words unchanged. (`next/app/(site)/tools/(hub)/page.tsx`) |
| `/work-alpha` | /work-alpha: the owner's research control room. (`next/app/(site)/work-alpha/page.tsx`) |

## Components (`next/components/`)

| file | what it is |
| --- | --- |
| `next/components/account/held.tsx` | everything this site keeps in this browser, in the words a reader would use, with what leaves the machine marked. |
| `next/components/account/kept.tsx` | what this account actually holds, listed. |
| `next/components/account/library.tsx` | the reading list and the notes. |
| `next/components/account/mirror.ts` | every key this account keeps, counted. |
| `next/components/account/paths.tsx` | where the reader is in each school. |
| `next/components/account/pref-swatch.tsx` | What pressing this would look like: every reading option carries a small picture of itself, because a row of chips reading "Com... |
| `next/components/account/prefs.tsx` | The five reading preferences, as a component rather than DOM built in a loop: pressed here, applied on every page immediately,... |
| `next/components/account/profile.ts` | the reader's own row, without fetching it twice. |
| `next/components/account/runtime.ts` | importing a module this site serves at a path. |
| `next/components/account/saved.tsx` | a filled-in calculator under a name. |
| `next/components/account/settings.tsx` | the three questions, and the same form either way. |
| `next/components/account/standing.ts` | how far through a school somebody is. |
| `next/components/account/targets.tsx` | a goal with a number on it. |
| `next/components/account/when.ts` | A date the way this page has always written one: "3 Aug 2026", and an empty string rather than "Invalid Date" for a row whose t... |
| `next/components/account/year.tsx` | the four numbers, and a year of days. |
| `next/components/admin/backups-panel.tsx` | Backups: what the nightly cron has actually written. |
| `next/components/admin/cards-panel.tsx` | every piece and every lesson gets a picture, without anybody deciding which. |
| `next/components/admin/courses-panel.tsx` | Courses: whether the third-party section works. |
| `next/components/admin/health.tsx` | is anything broken. The one panel on the admin page that needs no credential, and `functions/api/admin/[[route]].ts` says at le... |
| `next/components/admin/live-panel.tsx` | Live portfolio: what a stranger is being shown. |
| `next/components/admin/media-panel.tsx` | Media: what R2 is holding, and what nothing points at. |
| `next/components/admin/overview-panel.tsx` | Overview: what is waiting, and where it is. |
| `next/components/admin/panel.tsx` | One page, two credentials. |
| `next/components/admin/people-panel.tsx` | The one panel that needs both credentials. |
| `next/components/admin/pieces-panel.tsx` | Published: everything in the database, and what is wrong with it. |
| `next/components/admin/queue.tsx` | the three moderation panels, once. |
| `next/components/admin/queues.tsx` | The three queues, as three specs. |
| `next/components/admin/routine-panel.tsx` | Routine templates: the private ones. |
| `next/components/admin/row.tsx` | One line of a panel: a dot, a label, and what it says. |
| `next/components/admin/schools-panel.tsx` | Schools: the prose, as opposed to the ladder. |
| `next/components/admin/stats-panel.tsx` | Stats: what the site is being read for. |
| `next/components/admin/subscribers-panel.tsx` | Subscribers: the list, the confirmed count, and an export. |
| `next/components/admin/worker.tsx` | what THIS BROWSER is holding. |
| `next/components/cards.tsx` | a piece of writing, as a card. |
| `next/components/comments.tsx` | The thread under a piece. |
| `next/components/contact-form.tsx` | the contact form, and the three ways sending it can go. |
| `next/components/course-shell.tsx` | the empty page four course routes serve. |
| `next/components/courses/trail.tsx` | The whole path, down to the lesson. |
| `next/components/deck.tsx` | the two kinds of card, as components. |
| `next/components/diet/board.tsx` | today. The log on the left, the board on the upper right. `DIET.md` sections 11 and 24. This page has two jobs that pull in opp... |
| `next/components/diet/body.tsx` | Measurements in, composition out. |
| `next/components/diet/expect-panel.tsx` | What to expect, and when. |
| `next/components/diet/food-picker.tsx` | Food found rather than typed. |
| `next/components/diet/foods-panel.tsx` | The portion library, and what it costs. |
| `next/components/diet/forecast-panel.tsx` | a weight forecast built out of what the reader actually did. |
| `next/components/diet/glossary.tsx` | the words, in both languages. |
| `next/components/diet/goal-panel.tsx` | the engine, the floors, and the estimate. |
| `next/components/diet/habits-panel.tsx` | Seven daily things, read off what is already logged. |
| `next/components/diet/health-panel.tsx` | The numbers a clinic gives you, and the medicines that change what a chart means. |
| `next/components/diet/icons.ts` | one drawing per page of the diet tool. |
| `next/components/diet/import-panel.tsx` | arriving from another app. |
| `next/components/diet/insights-panel.tsx` | The readings that come out of a log, and what the food in it cost. |
| `next/components/diet/invite.tsx` | what a page says to somebody who is not signed in. |
| `next/components/diet/journal-panel.tsx` | what you are facing. `DIET.md` section 11. The journal is the only record of what this actually FELT like, and hunger is the on... |
| `next/components/diet/keto-panel.tsx` | Keto, while it is happening. |
| `next/components/diet/lang.tsx` | Both languages in the HTML, one on the screen. |
| `next/components/diet/log-form.tsx` | The left column. What you came here to do. `DIET.md` section 13: food diaries are abandoned because of FRICTION, not motivation... |
| `next/components/diet/nutrition-panel.tsx` | Beyond calories, and how honest it can be. |
| `next/components/diet/onboard.tsx` | the first ninety seconds. |
| `next/components/diet/page-frame.tsx` | the head every diet page has. |
| `next/components/diet/recipe-panel.tsx` | Build the dish once, then a portion of it is one tap for ever. |
| `next/components/diet/season-note.tsx` | the calendar, on the page. |
| `next/components/diet/strip.tsx` | the way from any page of this tool to any other. |
| `next/components/diet/summary-panel.tsx` | the page you take to a doctor. |
| `next/components/diet/trend-panel.tsx` | The long view. `DIET.md` sections 4, 10 and 28. The chart is an SVG drawn from the rows: no library and no canvas, and a chart... |
| `next/components/diet/usuals.tsx` | The four things that decide whether anybody keeps a food log at all. |
| `next/components/diet/widgets.tsx` | the board on the upper right. |
| `next/components/diet/words.ts` | The tool's vocabulary, said once: one place for every token a component needs a word for, which is the rule `check-rows.ts` alr... |
| `next/components/diet/year-panel.tsx` | A year in one page. `DIET.md` sections 10, 16 and 18. THE CHART IS THE TREND PAGE'S CHART and none of its decisions is retaken:... |
| `next/components/door.tsx` | The one thing on the front page that is the reader's: which school somebody is in the middle of, which is in their own browser. |
| `next/components/engage.tsx` | What a reader can do besides read: a row of three reactions, and a moderated question box with the questions already answered a... |
| `next/components/footer.tsx` | the end of every page. |
| `next/components/home/board.tsx` | The front page is a board the reader arranges. |
| `next/components/home/meters.tsx` | How far you are, per school, on the front page. |
| `next/components/home/reckoner.tsx` | The one thing on the front page a reader can use. |
| `next/components/home/writing.tsx` | The newest writing, on the front page. |
| `next/components/hub.tsx` | a reading section's index. |
| `next/components/icons.tsx` | one drawing set for the shell and the cards. |
| `next/components/keep.tsx` | Keep this page, and write in the margin of it: two controls in one row, under the byline of a piece and the meta line of a lesson. |
| `next/components/lesson/block.tsx` | one block, whichever kind it is. |
| `next/components/lesson/body.tsx` | two bodies, one set of blocks, in order. |
| `next/components/lesson/figure.tsx` | the ten drawings a lesson can carry. |
| `next/components/lesson/grid.tsx` | a sheet the reader types into. |
| `next/components/lesson/hear-prose.tsx` | touch any English line to hear it. |
| `next/components/lesson/hear.tsx` | one line, spoken. A small round button beside a line in the language being learnt. It is the one thing a reader of a language l... |
| `next/components/lesson/interactive.tsx` | the eight blocks a reader DOES something to. |
| `next/components/lesson/lab.tsx` | the two blocks made of numbers. |
| `next/components/lesson/lang-switch.tsx` | the one piece of the language pair that holds state. |
| `next/components/lesson/lang.tsx` | a lesson in two languages, one on the screen. |
| `next/components/lesson/language.tsx` | the four blocks a language lesson does. |
| `next/components/lesson/stars.tsx` | how much a lesson matters. |
| `next/components/market-pulse.tsx` | the board of headlines on the Insights hub. |
| `next/components/medals.tsx` | the shelf on a school hub. |
| `next/components/nav-tree.tsx` | The whole site, from the bar, on any page. |
| `next/components/netzwerk/bits.tsx` | the small pieces every planner panel uses. |
| `next/components/netzwerk/chapters.tsx` | The chapter map and the chapter tracker, as one list: every chapter opens to show what the map says about it and to take the tr... |
| `next/components/netzwerk/charts.tsx` | the planner's drawings. |
| `next/components/netzwerk/log.tsx` | The daily log and the weekly practice tracker: one line per session, thirty seconds at the end of block five, and one row per w... |
| `next/components/netzwerk/planner.tsx` | the study planner, whole. |
| `next/components/netzwerk/refs.tsx` | The four reference sheets: the daily method, the content library, the exams with their milestones, and the tips. |
| `next/components/netzwerk/start.tsx` | The Start sheet: who this learner is, which route, when they start, and the eight inputs every date and target follows. |
| `next/components/netzwerk/week.tsx` | This week, and the whole plan. |
| `next/components/news.tsx` | The headline card and the mini window. |
| `next/components/page.tsx` | a hand-written page's layout, in two lines. |
| `next/components/progress.tsx` | The reader's own ticks, on the page. |
| `next/components/pulse-card.tsx` | The writing, one piece at a time: a card on the front page that cycles through the latest live pieces and links to the one it i... |
| `next/components/read-aloud.tsx` | The speech control under a piece's byline. |
| `next/components/research/archive.tsx` | everything that happened, the bin, and the way out. |
| `next/components/research/ask.tsx` | the assistant. RESEARCH.md section 21. A task is picked from the list, given what it needs out of the studio's own rows, and se... |
| `next/components/research/atlas.tsx` | one graph of everything. |
| `next/components/research/board.tsx` | the front door, and the one page a reader opens every day. |
| `next/components/research/clip.tsx` | the bookmarklet's landing. |
| `next/components/research/field.tsx` | the field room. RESEARCH.md section 15. People, by pseudonym. A participant's real name, if kept at all, is sealed in this brow... |
| `next/components/research/files.tsx` | the files a source carries. |
| `next/components/research/find.tsx` | one search box, every index, one list. |
| `next/components/research/frame.tsx` | the head every room has. |
| `next/components/research/keys.ts` | the studio's single-letter keys, and the guard that makes them safe. |
| `next/components/research/lab.tsx` | the lab. RESEARCH.md section 14, and the campaign's additions in 36. A dataset is a file in R2 and a library source of type dat... |
| `next/components/research/lang.tsx` | both languages in the markup, one shown. |
| `next/components/research/library.tsx` | every source, and one page per source. |
| `next/components/research/method-lesson.tsx` | what sits under a lesson's prose. |
| `next/components/research/methods.tsx` | the methods room. RESEARCH.md section 20. A method is a piece with the tag `method`, so the room owns no rows: it draws the twe... |
| `next/components/research/notes.tsx` | the notebook. Six kinds of note, the daily log, the links between them, in the site's one editor: `createEditor()` out of `/edi... |
| `next/components/research/plan.tsx` | the planner. RESEARCH.md section 17. Six views over the rows the studio already keeps. The board is the tasks room's lanes with... |
| `next/components/research/questions.tsx` | the argument of the project, as a tree. |
| `next/components/research/queue.tsx` | what is waiting to be read. |
| `next/components/research/reader.tsx` | the reader. RESEARCH.md section 11. A PDF is drawn by pdf.js, self-hosted: the library is bundled by Next and its worker is a c... |
| `next/components/research/review.tsx` | the review room. RESEARCH.md 13. A review is a row holding the protocol, and what a search returns is a RECORD rather than a so... |
| `next/components/research/rooms-client.tsx` | the three rooms that carry a browser library, loaded in the browser and nowhere else. |
| `next/components/research/run.tsx` | one run, whole. RESEARCH.md section 14. A run is a page so a draft's figure or table can point at it: the label, the kind, the... |
| `next/components/research/settings.tsx` | projects, preferences, connections. |
| `next/components/research/signed-out.tsx` | A short invitation rather than an empty shell. |
| `next/components/research/source.tsx` | one source, as a form that saves as it is typed. |
| `next/components/research/strip.tsx` | the way from any room to any other. |
| `next/components/research/survey-form.tsx` | a survey, as a stranger sees it. |
| `next/components/research/tasks.tsx` | the five lanes, with no red in them. |
| `next/components/research/use-who.ts` | who is signed in, and staying up to date with it. |
| `next/components/research/workshop.tsx` | the workshop. RESEARCH.md section 19. Thirty small tools out of lib/research-tools.ts, one card each on the hub and one page ea... |
| `next/components/research/write.tsx` | the writing desk. RESEARCH.md section 16. A document is a row and the editor is the site's one editor, mounted the way the note... |
| `next/components/research.tsx` | The three research cards on the About page, and the window one of them opens. |
| `next/components/routine/account.tsx` | the routine, from the account page. |
| `next/components/routine/dashboard.tsx` | The routine, as a tool: a hero that states the answer, dense panels of real numbers, charts drawn from the reader's own data, a... |
| `next/components/routine/day.tsx` | the day, and the twenty seconds it has to fit into. |
| `next/components/routine/log.tsx` | One thing you did today, and the button for it. |
| `next/components/routine/print.tsx` | the week on paper. TWO THINGS, ONE BOOLEAN APART, and that is the whole design: BLANK is the paper fallback. Seven columns, the... |
| `next/components/routine/settings.tsx` | everything that shapes the tool. |
| `next/components/routine/year.tsx` | what a stretch of days has to say. |
| `next/components/school-contents.tsx` | every lesson of a school, on one page. |
| `next/components/school-hub-page.tsx` | the three hand-written school hubs, drawn from `lib/school-hub-content.ts`. |
| `next/components/school-hub.tsx` | A school's front page, out of its rows, counting what it drew. |
| `next/components/scripts.tsx` | the site's own modules, loaded after React has finished hydrating and not one moment before. |
| `next/components/sentence-game.tsx` | put the words back in order. |
| `next/components/shell.tsx` | The page around the page: the head, the rail, the top bar and the footer, once. |
| `next/components/sidebar.tsx` | The menu, down the left, on every page. |
| `next/components/stage-page.tsx` | A stage's contents page, rendered from its rows. |
| `next/components/subscribe.tsx` | the email box under the Insights hub's RSS line. |
| `next/components/topbar.tsx` | Who is reading, and nothing else. |
| `next/components/topic-filter.tsx` | the chips above the Insights cards, and the cards they hide. |
| `next/components/ui/band.tsx` | the block at the end of a page that asks for something. |
| `next/components/ui/button.tsx` | Every button on this site, once. |
| `next/components/ui/chip.tsx` | A small label, and the one rule about it. |
| `next/components/ui/crumbs.tsx` | The trail across the top of a page, server-rendered from a trail the route hands it: a route knows where it is, what it is call... |
| `next/components/ui/field.tsx` | A text box, and the label and the error it needs. |
| `next/components/ui/label.tsx` | the small line above a heading. |
| `next/components/ui/legend.tsx` | what the lines on a chart mean. |
| `next/components/ui/meter.tsx` | how far through something a reader is. |
| `next/components/ui/note.tsx` | the boxes a page puts an aside in. |
| `next/components/ui/stat.tsx` | A figure, and what it is a figure of. |
| `next/components/ui/surface.tsx` | what a thing is made of. |
| `next/components/ui/tab-panels.tsx` | a strip of choices where choosing one hides the rest. |
| `next/components/ui/tabs.tsx` | a strip of choices, and the two things a strip can be. |
| `next/components/used.tsx` | which calculators this reader actually opens. |
| `next/components/where.tsx` | how far into this piece you had got, and a way back to it. |
| `next/components/work-alpha/engine.ts` | Work-Alpha engine. One function, mount(root, plan, storage), draws the whole app from plan.json and a state object. PLAIN DOM O... |
| `next/components/work-alpha/mount.tsx` | The host for the engine, and the gate in front of it. |
| `next/components/work-alpha/owner-mark.tsx` | Keeps `data-owner` on `<html>` true, on every page, with nothing drawn. |
| `next/components/work-alpha/owner.ts` | Is this reader the owner? |
| `next/components/work-alpha/storage.ts` | The account is the record and the browser is a mirror. |
| `next/components/work-card.tsx` | one case study, as a card. |
| `next/components/workbook-body.tsx` | a whole practice book, as markup. |
| `next/components/workbook-page.tsx` | a whole practice book, as a route's body. |
| `next/components/workbook.tsx` | A day of a practice book, as a component: the markup a builder emitted, rendered by a route, which is what gives these pages th... |
| `next/components/written.tsx` | a page whose body is writing rather than data. |

## Libraries (`next/lib/`)

| file | what it is |
| --- | --- |
| `next/lib/admin-api.ts` | the passphrase half's endpoints, once. |
| `next/lib/art.ts` | the site's half of the drawing resolver. |
| `next/lib/article.ts` | one article, out of D1. |
| `next/lib/board.ts` | The board a reader arranged, in the browser. |
| `next/lib/cite.ts` | citeproc, and the document rendered through it. |
| `next/lib/crumbs.ts` | Where you are standing, out of the one table. |
| `next/lib/diet-api.ts` | The diet tool's rows, read and written as the reader. |
| `next/lib/diet-pages.ts` | every address of the diet tool, once. |
| `next/lib/duck.ts` | DuckDB in the browser, loaded once. |
| `next/lib/export-docx-table.ts` | the table maker's grid as Word, in the browser. |
| `next/lib/export-docx-tables.ts` | tables as Word, in the browser. |
| `next/lib/export-docx.ts` | a document as Word, in the browser. |
| `next/lib/export-pptx.ts` | a kind-`slides` document as PowerPoint. |
| `next/lib/flip.ts` | a window that grows out of the thing it was opened from. |
| `next/lib/hub.ts` | what a reading section's index page says about itself, and the prose around the list. |
| `next/lib/medal-stages.ts` | every stage's live lesson ids, per school, out of the four committed ladders. |
| `next/lib/medals.ts` | what a learner has earned, out of ticks they have. |
| `next/lib/metadata.ts` | the head of a hub page. |
| `next/lib/methods/` | 14 files: one method essay per file, for /tools/research/methods/<slug> |
| `next/lib/netzwerk-store.ts` | what the study planner keeps in a browser. |
| `next/lib/offline-files.ts` | a source's file kept on THIS device. |
| `next/lib/pageMeta.ts` | the head of a hand-written page. |
| `next/lib/pieces.ts` | what a reading section has in it, out of D1. |
| `next/lib/progress.ts` | What you have read, and where you were. |
| `next/lib/reader-api.ts` | the account half's endpoints, once. |
| `next/lib/recipes.ts` | The diet tool's arithmetic over recipes, pots, shares and prices. |
| `next/lib/research-api.ts` | the Research Studio's rows, read and written as the reader. |
| `next/lib/research-methods.ts` | the methods room's table. |
| `next/lib/research-pages.ts` | every room of the Research Studio, once. |
| `next/lib/research-tools.ts` | the workshop's table. RESEARCH.md 19. Thirty small tools, one card each, each a page under /tools/research/tools/<slug>. A tool... |
| `next/lib/school-hub-content.ts` | what the three hand-written school hubs say, as data. |
| `next/lib/school-hubs.ts` | the four schools' hand-written pages. |
| `next/lib/school-icons.ts` | GENERATED. Do not edit. node scripts/build-school-icons.ts The drawings the four schools put in a lesson's heading, copied out... |
| `next/lib/school-ladders.ts` | GENERATED by scripts/build-school-tree.ts. |
| `next/lib/school-stages.ts` | GENERATED by scripts/build-school-tree.ts. |
| `next/lib/school.ts` | One lesson of one school, out of D1, and the four schools' own words about themselves. |
| `next/lib/seal.ts` | a participant's name and contact, sealed in the browser. |
| `next/lib/sentence-game.ts` | The half of the sentence game a SERVER component needs: which of a day's lines can be played. |
| `next/lib/work.ts` | the seven case studies, as rows rather than as markup. |
| `next/lib/workbook.ts` | The practice books, read on the server. |
| `next/lib/workbooks/` | 5 files: one practice book per file, a term's or a Stufe's days |

## Shared (`shared/`)

| file | what it is |
| --- | --- |
| `shared/activity.ts` | what the reader's own movement is worth. |
| `shared/art-of.ts` | the drawing resolver, with the rail's two tables already handed to it. |
| `shared/art-svg.ts` | the twelve drawings and the six walls they stand against, as the inside of an `<svg>`. |
| `shared/art.ts` | which drawing a thing wears, and in what colour. |
| `shared/bundle.ts` | reading back a copy this site wrote. |
| `shared/calculators.ts` | the five calculators' arithmetic. |
| `shared/content.ts` | the site's content manifest. |
| `shared/courses.ts` | the third-party course catalogue. |
| `shared/csl/` | 11 files: the CSL citation styles the Research Studio renders |
| `shared/csv.ts` | a file somebody exported from another app, read. |
| `shared/curricula/deutsch.ts` | THE ONE COPY of the German school's ladder. |
| `shared/curricula/english.ts` | THE ONE COPY of the English school's ladder. |
| `shared/curricula/money.ts` | THE ONE COPY of the money school's ladder. |
| `shared/curricula/quran.ts` | THE ONE COPY of the Quranic Arabic school's ladder. |
| `shared/diet-words.ts` | The diet tool's own words. |
| `shared/diet.ts` | the arithmetic of the diet tool, said once. |
| `shared/foods.ts` | the portion library, for two kitchens. |
| `shared/headers.ts` | the security headers, for responses this code builds itself. |
| `shared/heads.ts` | What a hub page SAYS about itself: the eyebrow, the headline and the lede of every page that is a list rather than a piece of w... |
| `shared/insights.ts` | the readings a log earns. |
| `shared/lesson-grids.ts` | a sheet a reader types into. |
| `shared/lesson-labs.ts` | the arithmetic behind every `lab` block. |
| `shared/lesson.ts` | what an interactive part of a lesson is. |
| `shared/look.ts` | what a rendered piece looks like, per section. |
| `shared/nav.ts` | the whole menu, once. One table and three readers: the sidebar, the footer and the small-screen drawer, which are three VIEWS o... |
| `shared/netzwerk.ts` | the Netzwerk neu study planner, A1 to B2. |
| `shared/portfolio.ts` | what a broker's JSON means. |
| `shared/profile.ts` | What an account says about its reader, and the two vocabularies that describes. |
| `shared/research-assist.ts` | the assistant's tasks, its grounding and its cost. |
| `shared/research-bib.ts` | BibTeX and RIS, read and written. |
| `shared/research-field.ts` | the field room's vocabulary and arithmetic. |
| `shared/research-graph.ts` | the atlas's arithmetic. |
| `shared/research-lab.ts` | the lab's own arithmetic that is not a statistic. |
| `shared/research-plan.ts` | the planner's vocabulary and arithmetic. |
| `shared/research-review.ts` | the review room's vocabulary and arithmetic. |
| `shared/research-stats.ts` | the lab's first tier of statistics, in TypeScript, so it runs in every browser and in node. |
| `shared/research-tools.ts` | the workshop's arithmetic. |
| `shared/research-words.ts` | the Research Studio's own words. |
| `shared/research-write.ts` | the writing desk's arithmetic. |
| `shared/research.ts` | what the Research Studio's rows are made of. |
| `shared/routine.ts` | what a routine is, and the one calculation. |
| `shared/rows.ts` | what a row of this database actually is. |
| `shared/schools.ts` | the four curricula, out of the database, in the shape the site already speaks. |
| `shared/storage.ts` | everything this site keeps in a browser, in one table, with what it is and whether it leaves the machine. |
| `shared/tool-strings.ts` | every word on the calculators, twice. |
| `shared/widgets.ts` | The front page is a board the reader arranges. |

## Served modules (`aab/src/`, built to `aab/*.js`) and the schools' engines

| file | what it is |
| --- | --- |
| `aab/src/account-page.ts` | the four jobs left on /account after nine sections became components under `next/components/account/`. |
| `aab/src/account.ts` | who a reader is. Ordinary readers signing in to keep progress and to comment. NOT `auth.ts`, which guards the Studio with this... |
| `aab/src/activation.ts` | don't act on a page nobody has opened yet. |
| `aab/src/api.ts` | the browser's side of the dynamic layer. |
| `aab/src/app.ts` | sitewide behaviour. The theme (tri-state, swapped inside a View Transition), the Ctrl/Cmd+K palette built at runtime as a nativ... |
| `aab/src/audience.ts` | the front door. Two axes, not three doors: `audience` is "learn" or "work" (who you are) and `track` is "finance" or "skills" (... |
| `aab/src/auth-config.ts` | who is allowed into /studio/. |
| `aab/src/auth.ts` | the Studio's front door. |
| `aab/src/checkpoints.ts` | the ticks INSIDE a lesson, as against the one tick on it. |
| `aab/src/courses.ts` | the third-party course player. |
| `aab/src/editor.ts` | the writing surface, on its own. |
| `aab/src/photo.ts` | turning whatever an <img> points at into bytes this site can store. |
| `aab/src/pieces.ts` | what has been written, wherever it is kept. |
| `aab/src/prefs.ts` | how this reader wants to be read to. |
| `aab/src/routine.ts` | the browser's half of the routine. |
| `aab/src/saved.ts` | the things an account holds that are not a tick. |
| `aab/src/share-card.ts` | the picture a pasted link shows, drawn once at publish as a real 1200x630 JPEG. |
| `aab/src/signin.ts` | the button in the corner, and the menu behind it. |
| `aab/src/streak.ts` | which days somebody actually turned up, under `days-active`. |
| `aab/src/sync.ts` | progress belongs to the account, and the browser is a mirror. |
| `aab/src/tools/live.ts` | /tools/live.js: the live portfolio page. |
| `aab/src/tools/tools.ts` | the five calculators. Every one writes its state into the URL, so a result is a link. Charts are inline SVG drawn here, inherit... |
| `aab/schools/hub.js` | the parts of a school's front page that are the same drawing in every school. |
| `aab/schools/progress.js` | what a learner has read, which days they have done, and where they were. |
| `aab/schools/workbook.js` | a practice book, made daily. |

## API (`functions/`)

| file | what it is |
| --- | --- |
| `functions/_lib/admins.ts` | is this reader one of the site's own people? |
| `functions/_lib/assistant.ts` | the assistant's Worker half. |
| `functions/_lib/auth.ts` | the admin login. · the password is never stored: only PBKDF2-SHA256 at 210,000 iterations over a random 16-byte salt · comparis... |
| `functions/_lib/backup.ts` | a copy of the database that is not the database. |
| `functions/_lib/broker.ts` | talking to Trading 212, and keeping its keys. |
| `functions/_lib/climate.ts` | daily weather at a point, past years, through the Worker. |
| `functions/_lib/db.ts` | _lib/db.ts, D1 access, plus migrations that run themselves. |
| `functions/_lib/drive.test.ts` | the service account's half of `_lib/drive.ts`, and the ticket beside it. |
| `functions/_lib/drive.ts` | the one place this site reads Google Drive. |
| `functions/_lib/field.ts` | the field room's Worker half. |
| `functions/_lib/files.ts` | the Research Studio's files, in R2. |
| `functions/_lib/food.test.ts` | two food databases, read into one shape. |
| `functions/_lib/food.ts` | two public food databases, read as one shape. |
| `functions/_lib/http.ts` | the small conveniences every endpoint wants. |
| `functions/_lib/input.ts` | what a bad request looks like, decided once. |
| `functions/_lib/market.ts` | daily market series through the Worker. |
| `functions/_lib/notion.test.ts` | the Notion → HTML conversion. |
| `functions/_lib/notion.ts` | _lib/notion.ts, turning a Notion page into this site's HTML. |
| `functions/_lib/quiz.test.ts` | a Coursera quiz export, turned into questions. |
| `functions/_lib/quiz.ts` | a Coursera quiz export, turned into questions. |
| `functions/_lib/r2.ts` | what an R2 bucket is, said once. |
| `functions/_lib/reader.ts` | proving who a reader is, on the server. |
| `functions/_lib/sanitise.ts` | server-side HTML cleaning. |
| `functions/_lib/scholar-search.ts` | one search box, every index, one list. |
| `functions/_lib/scholar.ts` | the world's indexes, read as one shape. |
| `functions/_lib/sync.ts` | _lib/sync.ts, keeping Notion and the site in step. |
| `functions/_lib/ticket.ts` | a short-lived pass for one file. |
| `functions/_lib/workshop.ts` | the workshop's three lookups that need another server. |
| `functions/api/admin/[[route]].ts` | /api/admin/* `health` answers TWO different things depending on whether the caller has a credential, because the panel has to b... |
| `functions/api/articles/[[slug]].ts` | /api/articles: the CMS. |
| `functions/api/auth/[[route]].ts` | /api/auth/*, setup, login, logout, me GET /api/auth/params → { configured, scheme, salt, iterations } POST /api/auth/setup { sa... |
| `functions/api/backup/[[route]].ts` | /api/backup: reading the backups out. |
| `functions/api/broker/[[route]].ts` | /api/broker/*: the live portfolio, proxied and metered. |
| `functions/api/comments/[[id]].ts` | /api/comments: a thread under a piece, moderated. |
| `functions/api/courses/[[route]].ts` | /api/courses/*: the third-party course catalogue, to an admin and to nobody else. |
| `functions/api/diet/[[route]].ts` | /api/diet/*: food found rather than typed. |
| `functions/api/engine/[[route]].ts` | /api/engine/<package>/<version>/<file>: the lab's engine, served from this origin. |
| `functions/api/enquiries/[[id]].ts` | /api/enquiries: the client pipeline. |
| `functions/api/foods.ts` | functions/api/foods.ts, served at /api/foods. |
| `functions/api/media/[[key]].ts` | /api/media, where the photos actually live. |
| `functions/api/news.ts` | functions/api/news.ts, Cloudflare Pages Function Served automatically at /api/news (the /functions folder maps to URL routes). |
| `functions/api/notion/[[route]].ts` | /api/notion, write in Notion, publish here. |
| `functions/api/place.ts` | functions/api/place.ts, served at /api/place A town by name: "Sylhet" in, a short list of names and coordinates out, for the re... |
| `functions/api/questions/[[id]].ts` | /api/questions, reader questions, moderated. |
| `functions/api/research/[[route]].ts` | /api/research/*: the Research Studio's Worker surface. |
| `functions/api/routine/[[route]].ts` | /api/routine/* ONE ROUTE, and it exists for one reason: a template that is a real person's day rather than a suggestion. |
| `functions/api/schools/[[route]].ts` | /api/schools, the four curricula out of the database. |
| `functions/api/search.ts` | /api/search?q=, search that reads the articles themselves. |
| `functions/api/signals/[[kind]].ts` | /api/signals, analytics that can't identify anybody. |
| `functions/api/site.ts` | functions/api/site.ts, served at /api/site. |
| `functions/api/subscribers/[[route]].ts` | /api/subscribers: the list you own. |
| `functions/api/survey/[[route]].ts` | /api/survey/<token>: the public half of a field room survey. |
| `functions/api/tools.ts` | functions/api/tools.ts, served at /api/tools. |
| `functions/api/work-alpha.ts` | /api/work-alpha: is this reader the owner of the control room? |
| `functions/feeds/[kind].ts` | /feed.xml and /sitemap.xml: the static files, plus the database. |
| `functions/insights/[slug].ts` | /<section>/<slug>, renders an article that lives in the database, and gets out of the way for one that doesn't. |

## Scripts: checks, builds, seeds (`scripts/`)

| file | what it is |
| --- | --- |
| `scripts/build-fallback.ts` | the stylesheet for the two pages that cannot have one. |
| `scripts/build-map.ts` | MAP.md, the one note an agent reads first. |
| `scripts/build-meta.ts` | build-meta.ts, regenerates the machine-readable files from the site's manifest, so they can never drift out of date: feed.xml R... |
| `scripts/build-modules.ts` | the site's own modules, from TypeScript. |
| `scripts/build-og.ts` | build-og.ts, renders the social share images. |
| `scripts/build-school-icons.ts` | the schools' drawings, where `next/` can reach them. |
| `scripts/build-school-tree.ts` | the four ladders, for the two places that need them and cannot ask the database. |
| `scripts/build-stamp.ts` | the React workspace's output really is its source's. |
| `scripts/check-accents.ts` | one table decides what colour a page wears. |
| `scripts/check-account.ts` | leaving takes everything with it. |
| `scripts/check-admin.ts` | every endpoint is gated, or is public on purpose and says why. |
| `scripts/check-all.ts` | every check and every fast test, in one command. |
| `scripts/check-api.ts` | does the browser call routes that exist? |
| `scripts/check-app-surface.ts` | does a table this site holds reach the app, or is it written down that it does not? |
| `scripts/check-art.ts` | a drawing may name nine colours, and every one of them has to be a colour somebody resolves. |
| `scripts/check-calculators.ts` | does every sentence have its numbers? |
| `scripts/check-closed.ts` | the old system is a CLOSED SET. |
| `scripts/check-components.ts` | a component only updates the pages that use it. |
| `scripts/check-content.ts` | a page that has stopped telling the truth about the site. |
| `scripts/check-contrast.ts` | the palette is readable, and it is measured rather than believed. |
| `scripts/check-courses.ts` | the third-party course section, held to the eight things that are easy to get quietly wrong. |
| `scripts/check-crons.ts` | the schedules in wrangler.toml are the ones worker.js is waiting for. |
| `scripts/check-csp.ts` | can the browser actually reach what the code asks for? |
| `scripts/check-css.ts` | a school's styles leaking into the whole site, and everything else this stylesheet can get wrong as text. |
| `scripts/check-dashes.ts` | has an em dash got in? |
| `scripts/check-diet.ts` | DIET.md's page rules, the ones neither scripts/diet.test.ts (the arithmetic) nor next/diet.test.ts (the browser) can see. |
| `scripts/check-english.ts` | is the grammar term still whole? |
| `scripts/check-headers.ts` | do the two places that set security headers still say the same thing? |
| `scripts/check-icons.ts` | an icon name that draws nothing. |
| `scripts/check-jsx-nesting.ts` | a paragraph holding something a paragraph cannot hold, which is how a page loses hydration. |
| `scripts/check-jsx-space.ts` | the space between a sentence and the link inside it, which JSX eats. |
| `scripts/check-live.ts` | is the site that is actually deployed doing what the repository thinks it is? |
| `scripts/check-migrations.ts` | SQL that the runner will refuse. |
| `scripts/check-mjs.ts` | nothing here is `.mjs`. |
| `scripts/check-money.ts` | is the money school still the thing MONEY.md describes? |
| `scripts/check-next.ts` | the things `next/` has a second copy of. |
| `scripts/check-pieces.ts` | where every written piece actually lives. |
| `scripts/check-plain.ts` | what the stylesheet spends on paint, as a list. |
| `scripts/check-pointers.ts` | does a comment naming a file name one that exists? |
| `scripts/check-prefixes.ts` | the build writes the vendor prefixes, and a hand-written one can delete the property it was meant to help. |
| `scripts/check-prefs.ts` | a preference is written out twice, and the two copies have to say the same thing. |
| `scripts/check-preview.ts` | does the Next.js Worker's branch preview render what the live site renders? |
| `scripts/check-research.ts` | the Research Studio's rules that are about PAGES and TABLES rather than about arithmetic. |
| `scripts/check-rls.ts` | every table in Supabase is somebody's, and the ones that are not say so out loud. |
| `scripts/check-routes.ts` | broken URLs, before deploying. |
| `scripts/check-rows.ts` | does `shared/rows.ts` still describe this database, and do the handlers still agree with it? |
| `scripts/check-scale.ts` | the type scale is the type scale. |
| `scripts/check-schools.ts` | does the snapshot still describe the same four schools the ladders do? |
| `scripts/check-selfref.ts` | a custom property set to itself is a cycle, and the browser throws the whole declaration out. |
| `scripts/check-storage.ts` | everything this site keeps in a browser is described, and what it says about itself is true. |
| `scripts/check-surfaces.ts` | a card is still a card. |
| `scripts/check-sw.ts` | did a precached file change without a VERSION bump? |
| `scripts/check-types.ts` | the node-side TypeScript, typechecked. |
| `scripts/check-utility-clash.ts` | a class this site styles must not also be a class Tailwind generates. |
| `scripts/check-worker-size.ts` | the Next Worker against Cloudflare's cap. |
| `scripts/english/shape.ts` | what one authored part looks like. |
| `scripts/english/term-3/` | 28 files: the English grammar term's seeded parts, one part per file, and the three rungs that gather them |
| `scripts/english/term-3.ts` | টার্ম ৩: ইংরেজি ব্যাকরণ. |
| `scripts/export-calculator-fixtures.ts` | the five calculators, frozen. |
| `scripts/export-diet-fixtures.ts` | the body and the energy, frozen. |
| `scripts/export-portfolio-fixtures.ts` | the broker derivations, frozen. |
| `scripts/export-routine-fixtures.ts` | the routine's arithmetic, frozen. |
| `scripts/export-schools.ts` | the schools out of D1 and into git. |
| `scripts/export-stock-fixtures.ts` | the stock model's answers, frozen. |
| `scripts/import-courses.ts` | the third-party course catalogue, from the Drive folder it lives in. |
| `scripts/import-csl.ts` | the citation styles, vendored as strings. |
| `scripts/import-schools.ts` | the four curricula, as SQL. |
| `scripts/lib/coursera.ts` | coursera.mjs: how a Drive folder of course files becomes a course, a module and a lesson. |
| `scripts/lib/css-tokens.ts` | what a token in `styles.css` actually is, as a colour, in one mode. |
| `scripts/money/` | 5 files: the money school's seeded lessons, one stage per file |
| `scripts/preview.ts` | every surface this site has, on one page. |
| `scripts/restore.ts` | turning a backup back into a database. |
| `scripts/school-source.ts` | where a builder gets its curriculum. |
| `scripts/schools-snapshot.ts` | the schools' rows, as a file a builder can read with no network. |
| `scripts/seed-english.ts` | the English school's rows, out of the ladder and the prose beside it. |
| `scripts/seed-money.ts` | the money school's rows, out of the ladder and the prose beside it. |
| `scripts/sqlite-d1.ts` | the D1 binding, over node:sqlite. |

## The Studio (`app/src/`)

| file | what it is |
| --- | --- |
| `app/src/api.ts` | the site's API, with its shapes written down. |
| `app/src/bits.tsx` | what the desk and the Studio need that the site's component library does not have yet. |
| `app/src/site.ts` | what this app borrows from the site. |
| `app/src/studio/Editor.tsx` | the one part of this page React does not own. |
| `app/src/studio/Fields.tsx` | everything about the piece that is not the piece. |
| `app/src/studio/Lessons.tsx` | the schools' prose, edited where it lives. |
| `app/src/studio/Preflight.tsx` | before it goes out. An empty editor is not a problem worth shouting about yet, so the panel does not appear until something has... |
| `app/src/studio/Preview.tsx` | what a reader actually meets. |
| `app/src/studio/Sheets.tsx` | Open, and Import from Notion. |
| `app/src/studio/Studio.tsx` | paste the article, paste the photos, publish. |
| `app/src/studio/drafts.ts` | what is on this device. |
| `app/src/studio/lessons.ts` | the four schools, as the Studio talks to them. |
| `app/src/studio/main.tsx` | the gate, then React. The auth gate is the site's own `requireOwner()` from `/auth.js`, unchanged and un-ported. It is the one... |
| `app/src/studio/notion.ts` | the three Notion calls, typed. |
| `app/src/studio/piece.ts` | what is in the editor, and what it will become. |
| `app/src/studio/preflight.ts` | the things an editor checks before a piece goes out, checked every time instead of when someone remembers. |
| `app/src/studio/publish.ts` | photos, then the card, then the article. |

## Tests

Every `*.test.ts`. A test that needs a browser or a build says so in its header and skips out loud without one.

| file | what it guards |
| --- | --- |
| `aab/courses.test.ts` | the third-party course player, driven. |
| `aab/editor.test.ts` | `aab/editor.js`, driven in a real browser. |
| `aab/portfolio/dissertation.test.ts` | checks on the statistics engine. |
| `aab/portfolio/frontier.test.ts` | checks on the portfolio-construction engine. |
| `aab/portfolio/scorecard.test.ts` | checks on the PD modelling pipeline. |
| `aab/portfolio/stress.test.ts` | checks on the credit stress-testing engine. |
| `aab/schools/hub.test.ts` | the three school hubs, against the markup a reader actually gets. |
| `aab/schools/progress.test.ts` | what the three schools' progress modules do. |
| `aab/schools/workbook.test.ts` | both practice books, against the markup a learner actually gets. |
| `aab/studio-publish.test.ts` | a pasted photo actually reaches /media. |
| `aab/sw.test.ts` | the one file whose name never changes. |
| `aab/sync.test.ts` | progress belongs to the account. |
| `app/studio.test.ts` | the React Studio, driven in a real browser. |
| `functions/_lib/drive.test.ts` | the service account's half of `_lib/drive.ts`, and the ticket beside it. |
| `functions/_lib/food.test.ts` | two food databases, read into one shape. |
| `functions/_lib/notion.test.ts` | the Notion → HTML conversion. |
| `functions/_lib/quiz.test.ts` | a Coursera quiz export, turned into questions. |
| `next/account.test.ts` | What an account actually gets you, driven in a real browser. |
| `next/admin.test.ts` | /admin in a real browser. |
| `next/article.test.ts` | The article page, in a browser, on the real Worker. |
| `next/book-api.test.ts` | The practice book, without its answer key. |
| `next/comments.test.ts` | the thread under a piece, as markup. |
| `next/diet.test.ts` | the diet tool, in a real browser. |
| `next/insights-hub.test.ts` | the two pieces of behaviour the Insights hub has, in a browser. |
| `next/interactive.test.ts` | Does what a page's own module writes into it survive the page being a route? |
| `next/keep.test.ts` | The Save and the note under a byline, in a browser. |
| `next/lesson.test.ts` | Every kind of lesson block, and every model behind a lab, server rendered and then hydrated in a real browser. |
| `next/market-pulse.test.ts` | the board of headlines on the Insights hub, in a browser. |
| `next/medals.test.ts` | The medals, which are arithmetic over the ticks. |
| `next/parity.test.ts` | Does the Next.js route render the same article as the Worker, and do the hubs and school pages agree with the database? |
| `next/progress.test.ts` | The money school's ticks. |
| `next/read-aloud.test.ts` | the speech control on a piece, in a browser. |
| `next/reading.test.ts` | Is a piece of writing actually readable. |
| `next/recipes.test.ts` | A dish built once, and the three-tap log it unlocks. |
| `next/research-studio.test.ts` | the Research Studio, in a browser. |
| `next/research.test.ts` | The research window on the About page, in a browser. |
| `next/routine-day.test.ts` | the day, in a browser, signed in. |
| `next/tracking.test.ts` | What this site records about a reader, in a browser. |
| `next/work-alpha.test.ts` | The owner's research control room, in two halves. |
| `scripts/activity.test.ts` | the movement arithmetic, as assertions. |
| `scripts/admin.test.ts` | the admin panel's constraints, as assertions. |
| `scripts/bundle.test.ts` | reading back a copy this site wrote. |
| `scripts/comments.test.ts` | the endpoint, against real SQLite and real signatures. |
| `scripts/csv.test.ts` | the importer, from the wrong side. |
| `scripts/diet.test.ts` | the diet tool's arithmetic, as assertions. |
| `scripts/input.test.ts` | what a bad request looks like, tested. |
| `scripts/insights.test.ts` | section 16's readings and section 17's money, as assertions. |
| `scripts/netzwerk.test.ts` | the study planner's plan is the workbook's. |
| `scripts/reader.test.ts` | the signature check, and the attacks. |
| `scripts/research-assist.test.ts` | the assistant's pure half. |
| `scripts/research-field.test.ts` | the field room's arithmetic. |
| `scripts/research-stats.test.ts` | the lab's arithmetic held to closed forms. |
| `scripts/research-tools.test.ts` | the workshop's arithmetic held to the numbers a textbook gives. |
| `scripts/research.test.ts` | the Research Studio's arithmetic, as assertions. |
| `scripts/restore.test.ts` | the backup round trip, for real. |
| `scripts/routine.test.ts` | the product, as assertions. |
| `scripts/schools-api.test.ts` | /api/schools, against real SQLite. |
| `scripts/schools.test.ts` | does a curriculum survive the round trip? |
| `scripts/site-api.test.ts` | /api/site, the app's view of the site's own furniture. |
| `scripts/snapshot.test.ts` | the R2 half of the backup. |
| `scripts/widgets.test.ts` | the board a reader arranges. |

## What runs in `check-all.ts`

| entry | what it catches |
| --- | --- |
| `scripts/check-types.ts` | the node-side TypeScript, typechecked. |
| `scripts/check-pointers.ts` | does a comment naming a file name one that exists? |
| `scripts/check-routes.ts` | broken URLs, before deploying. |
| `scripts/check-css.ts` | a school's styles leaking into the whole site, and everything else this stylesheet can get wrong as text. |
| `scripts/check-sw.ts` | did a precached file change without a VERSION bump? |
| `scripts/check-content.ts` | a page that has stopped telling the truth about the site. |
| `scripts/check-calculators.ts` | does every sentence have its numbers? |
| `scripts/check-csp.ts` | can the browser actually reach what the code asks for? |
| `scripts/check-crons.ts` | the schedules in wrangler.toml are the ones worker.js is waiting for. |
| `scripts/check-pieces.ts` | where every written piece actually lives. |
| `scripts/check-headers.ts` | do the two places that set security headers still say the same thing? |
| `scripts/check-schools.ts` | does the snapshot still describe the same four schools the ladders do? |
| `scripts/check-money.ts` | is the money school still the thing MONEY.md describes? |
| `scripts/check-english.ts` | is the grammar term still whole? |
| `scripts/check-rows.ts` | does `shared/rows.ts` still describe this database, and do the handlers still agree with it? |
| `scripts/check-rls.ts` | every table in Supabase is somebody's, and the ones that are not say so out loud. |
| `scripts/check-migrations.ts` | SQL that the runner will refuse. |
| `scripts/check-api.ts` | does the browser call routes that exist? |
| `scripts/check-app-surface.ts` | does a table this site holds reach the app, or is it written down that it does not? |
| `scripts/check-contrast.ts` | the palette is readable, and it is measured rather than believed. |
| `scripts/check-surfaces.ts` | a card is still a card. |
| `scripts/check-components.ts` | a component only updates the pages that use it. |
| `scripts/check-jsx-space.ts` | the space between a sentence and the link inside it, which JSX eats. |
| `scripts/check-jsx-nesting.ts` | a paragraph holding something a paragraph cannot hold, which is how a page loses hydration. |
| `scripts/check-scale.ts` | the type scale is the type scale. |
| `scripts/check-prefixes.ts` | the build writes the vendor prefixes, and a hand-written one can delete the property it was meant to help. |
| `scripts/check-selfref.ts` | a custom property set to itself is a cycle, and the browser throws the whole declaration out. |
| `scripts/check-utility-clash.ts` | a class this site styles must not also be a class Tailwind generates. |
| `scripts/check-closed.ts` | the old system is a CLOSED SET. |
| `scripts/check-admin.ts` | every endpoint is gated, or is public on purpose and says why. |
| `scripts/check-prefs.ts` | a preference is written out twice, and the two copies have to say the same thing. |
| `scripts/check-storage.ts` | everything this site keeps in a browser is described, and what it says about itself is true. |
| `scripts/check-account.ts` | leaving takes everything with it. |
| `scripts/check-art.ts` | a drawing may name nine colours, and every one of them has to be a colour somebody resolves. |
| `scripts/check-icons.ts` | an icon name that draws nothing. |
| `scripts/check-mjs.ts` | nothing here is `.mjs`. |
| `scripts/check-dashes.ts` | has an em dash got in? |
| `scripts/check-next.ts` | the things `next/` has a second copy of. |
| `scripts/check-courses.ts` | the third-party course section, held to the eight things that are easy to get quietly wrong. |
| `scripts/check-diet.ts` | DIET.md's page rules, the ones neither scripts/diet.test.ts (the arithmetic) nor next/diet.test.ts (the browser) can see. |
| `scripts/check-research.ts` | the Research Studio's rules that are about PAGES and TABLES rather than about arithmetic. |
| `scripts/check-accents.ts` | one table decides what colour a page wears. |
| `scripts/build-map.ts` | MAP.md, the one note an agent reads first. |
| `scripts/build-modules.ts` | the site's own modules, from TypeScript. |
| `scripts/build-fallback.ts` | the stylesheet for the two pages that cannot have one. |
| `scripts/build-school-icons.ts` | the schools' drawings, where `next/` can reach them. |
| `scripts/build-stamp.ts` | the React workspace's output really is its source's. |
| `scripts/import-courses.ts` | the third-party course catalogue, from the Drive folder it lives in. |
| `scripts/export-stock-fixtures.ts` | the stock model's answers, frozen. |
| `scripts/export-calculator-fixtures.ts` | the five calculators, frozen. |
| `scripts/export-portfolio-fixtures.ts` | the broker derivations, frozen. |
| `scripts/export-routine-fixtures.ts` | the routine's arithmetic, frozen. |
| `scripts/export-diet-fixtures.ts` | the body and the energy, frozen. |
| `scripts/input.test.ts` | what a bad request looks like, tested. |
| `scripts/reader.test.ts` | the signature check, and the attacks. |
| `scripts/comments.test.ts` | the endpoint, against real SQLite and real signatures. |
| `scripts/restore.test.ts` | the backup round trip, for real. |
| `scripts/bundle.test.ts` | reading back a copy this site wrote. |
| `scripts/snapshot.test.ts` | the R2 half of the backup. |
| `scripts/routine.test.ts` | the product, as assertions. |
| `scripts/netzwerk.test.ts` | the study planner's plan is the workbook's. |
| `scripts/diet.test.ts` | the diet tool's arithmetic, as assertions. |
| `scripts/research.test.ts` | the Research Studio's arithmetic, as assertions. |
| `scripts/insights.test.ts` | section 16's readings and section 17's money, as assertions. |
| `scripts/widgets.test.ts` | the board a reader arranges. |
| `next/recipes.test.ts` | A dish built once, and the three-tap log it unlocks. |
| `scripts/admin.test.ts` | the admin panel's constraints, as assertions. |
| `scripts/schools.test.ts` | does a curriculum survive the round trip? |
| `scripts/schools-api.test.ts` | /api/schools, against real SQLite. |
| `scripts/site-api.test.ts` | /api/site, the app's view of the site's own furniture. |
| `functions/_lib/notion.test.ts` | the Notion → HTML conversion. |
| `functions/_lib/drive.test.ts` | the service account's half of `_lib/drive.ts`, and the ticket beside it. |
| `functions/_lib/quiz.test.ts` | a Coursera quiz export, turned into questions. |
| `functions/_lib/food.test.ts` | two food databases, read into one shape. |
| `aab/schools/progress.test.ts` | what the three schools' progress modules do. |
| `next/progress.test.ts` | The money school's ticks. |
| `next/medals.test.ts` | The medals, which are arithmetic over the ticks. |
| `next/comments.test.ts` | the thread under a piece, as markup. |
| `next/book-api.test.ts` | The practice book, without its answer key. |
| `aab/schools/hub.test.ts` | the three school hubs, against the markup a reader actually gets. |
| `aab/schools/workbook.test.ts` | both practice books, against the markup a learner actually gets. |
| `aab/courses.test.ts` | the third-party course player, driven. |
| `aab/portfolio/stress.test.ts` | checks on the credit stress-testing engine. |
| `aab/portfolio/scorecard.test.ts` | checks on the PD modelling pipeline. |
| `aab/portfolio/frontier.test.ts` | checks on the portfolio-construction engine. |
| `aab/portfolio/dissertation.test.ts` | checks on the statistics engine. |
| `scripts/csv.test.ts` | the importer, from the wrong side. |
| `scripts/research-stats.test.ts` | the lab's arithmetic held to closed forms. |
| `scripts/research-field.test.ts` | the field room's arithmetic. |
| `scripts/research-tools.test.ts` | the workshop's arithmetic held to the numbers a textbook gives. |
| `scripts/research-assist.test.ts` | the assistant's pure half. |
| `scripts/activity.test.ts` | the movement arithmetic, as assertions. |

## Storage keys (`shared/storage.ts`)

What a browser holds, which kind it is, whether it syncs, and who writes it.

| key | what it is |
| --- | --- |
| `research-dense` | cache: Whether the Research Studio draws itself with less air around everything, copied from your account so the first paint can read it. (next/components/research/settings.tsx) |
| `learn-read` | progress, syncs: The money school's lessons you have ticked. (next/lib/progress.ts) |
| `learn-last` | progress, syncs: The last money lesson you opened. (next/lib/progress.ts) |
| `learn-checks` | progress, syncs: The checkpoints you have ticked inside money lessons. (aab/checkpoints.js) |
| `deutsch-read` | progress, syncs: The German lessons you have ticked. (aab/deutsch/progress.js) |
| `deutsch-days` | progress, syncs: The German practice days you have finished. (aab/deutsch/progress.js) |
| `deutsch-last` | progress, syncs: The last German lesson you opened. (aab/deutsch/progress.js) |
| `deutsch-tag` | progress, syncs: How far into the German practice book you have got. (aab/deutsch/progress.js) |
| `deutsch-checks` | progress, syncs: The checkpoints you have ticked inside German lessons. (aab/checkpoints.js) |
| `english-read` | progress, syncs: The English lessons you have ticked. (aab/english/progress.js) |
| `english-days` | progress, syncs: The English practice days you have finished. (aab/english/progress.js) |
| `english-last` | progress, syncs: The last English lesson you opened. (aab/english/progress.js) |
| `english-day` | progress, syncs: How far into the English practice book you have got. (aab/english/progress.js) |
| `english-checks` | progress, syncs: The checkpoints you have ticked inside English lessons. (aab/checkpoints.js) |
| `quran-done` | progress, syncs: The Qur'anic Arabic lessons you have ticked. (aab/quran/progress.js) |
| `quran-last` | progress, syncs: The last Qur'anic Arabic lesson you opened. (aab/quran/progress.js) |
| `quran-checks` | progress, syncs: The checkpoints you have ticked inside Qur'anic Arabic lessons. (aab/checkpoints.js) |
| `courses-read` | progress, syncs: The course lessons you have marked complete. (aab/src/courses.ts) |
| `courses-last` | progress, syncs: The last course lesson you opened. (aab/src/courses.ts) |
| `courses-answers` | progress, syncs: What you picked in a course quiz. Nothing is marked right or wrong. (aab/src/courses.ts) |
| `deutsch-schrift` | made, syncs: What you wrote in the German practice book. (aab/deutsch/arbeitsbuch.js) |
| `english-write` | made, syncs: What you wrote in the English practice book. (aab/english/workbook.js) |
| `netzwerk-plan` | made, syncs: Your Netzwerk neu study plans: each learner's route, start date, targets, ticks, daily log and weekly scores. (next/lib/netzwerk-store.ts) |
| `netzwerk-active` | device: Which learner the study planner had open in this tab. (next/lib/netzwerk-store.ts) |
| `days-active` | progress, syncs: The days you turned up. (aab/src/streak.ts) |
| `where-read` | progress, syncs: How far into each piece and lesson you had got. (next/lib/progress.ts) |
| `tools-used` | progress, syncs: When you last opened each calculator. (next/lib/progress.ts) |
| `home-board` | made, syncs: How you arranged your front page. (next/lib/board.ts) |
| `drafts` | made: Unpublished writing in the Studio. (app/src/studio/drafts.ts) |
| `work-alpha` | made: Every tick, note and row of the Work-Alpha month plan, mirrored here so a save made offline is not lost. (next/components/work-alpha/storage.ts) |
| `reader-prefs` | preference, syncs: Type size, line width, the finish, and which language the calculators open in. (aab/src/prefs.ts) |
| `theme` | preference: Light, dark, or whatever this machine is set to. (aab/src/app.ts) |
| `tool-lang` | preference: Which language the calculators open in. (aab/src/prefs.ts) |
| `tool-depth` | preference: Whether a calculator opens with its main numbers or all of them. (aab/src/prefs.ts) |
| `audience` | preference: Whether the menu leads with learning or with hiring. (aab/src/audience.ts) |
| `track` | preference: Which learning track you said you were on. (aab/src/audience.ts) |
| `rail` | device: Whether the menu down the left is open or folded. (next/components/sidebar.tsx) |
| `research-files` | device: The research files you chose to keep on this device, for reading offline. (next/lib/offline-files.ts) |
| `reiad-session` | session: Your sign-in, so you are still signed in tomorrow. (aab/src/account.ts) |
| `reiad-profile` | cache: Your name and settings, so the account page can draw before the network answers. (aab/src/account.ts) |
| `sync-base` | session: What the account said at the last exchange, so two devices can be reconciled. (aab/src/sync.ts) |
| `work-alpha-owner` | cache: Whether /api/work-alpha last said you are the site's owner, so the rail can draw that entry before the first paint. (next/components/work-alpha/owner.ts) |
| `pulse-cache` | cache: The last market headlines, so the board is not empty while it fetches. (next/components/news.tsx) |
| `studio-unlocked-local` | session: That you unlocked the Studio in this tab. (aab/src/auth.ts) |
| `broker-tab-key` | session: A broker key you pasted for this tab only, never stored. (aab/src/tools/live.ts) |
| `studio-unlocked` | legacy: A Studio unlock that used to outlive the tab. (aab/src/auth.ts) |

## The menu (`shared/nav.ts`)

| address | entry (group) |
| --- | --- |
| `/skills` | All skills (learn) |
| `/money` | Money (learn) |
| `/deutsch` | German (learn) |
| `/quran` | Qur'anic Arabic (learn) |
| `/english` | English (learn) |
| `/cooking` | Cooking (learn) |
| `/travel` | Travel (learn) |
| `/skills/courses` | Courses (learn) |
| `/skills#reviews` | Reviews (learn) |
| `/tools` | Calculators (make) |
| `/tools/stock` | Stock check (make) |
| `/tools/live` | Live portfolio (make) |
| `/tools/routine` | Routine (make) |
| `/tools/diet` | Diet (make) |
| `/tools/research` | Research (make) |
| `/insights` | Insights (read) |
| `/portfolio` | Portfolio (work) |
| `/about` | About (work) |
| `/contact` | Contact (work) |
| `/account` | Account (you) |
| `/admin` | Admin (you) |
| `/work-alpha` | Work-Alpha (you) |
