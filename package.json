{
  "name": "reiad-website",
  "private": true,
  "type": "module",
  "//": [
    "The repository's own build tools, and nothing else. This site",
    "has no build step in CI: it deploys by uploading aab/. The",
    "other three package.json files are real workspaces (app/,",
    "next/, shared/).",
    "",
    "Nothing here is shipped: no dependency below is imported by",
    "any file the browser or either Worker loads. linkedom, react,",
    "react-dom and esbuild are for aab/schools/hub.test.ts, which",
    "renders the three hubs with no browser and no network. Same",
    "versions as next/ on purpose, and at the ROOT because CI runs",
    "`npm ci` here and nowhere else: a test resolving out of",
    "next/node_modules passes on a laptop and fails on the runner.",
    "",
    "`deploy` is not a build step. It is the deploy command written",
    "where somebody looking for it will find it, because a",
    "Cloudflare build command filled in with the familiar one fails",
    "on `Missing script: \"deploy\"`."
  ],
  "scripts": {
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/node": "^26.2.0",
    "esbuild": "^0.25.4",
    "linkedom": "^0.18.13",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3"
  }
}
