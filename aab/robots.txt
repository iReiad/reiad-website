# Reiad's Library: Finance & Bangladesh markets
User-agent: *
Allow: /

# The publishing tool and the admin panel, and what they collected,
# are mine rather than content. No trailing slash: the two pages are
# /studio and /admin, and "Disallow: /studio/" is a prefix that
# covers neither. /desk was the third and is a 301 to /admin now,
# so a crawler that follows the old address lands inside this rule.
Disallow: /studio
Disallow: /admin
Disallow: /work-alpha
Disallow: /insights/_template.html

# One person's own copy of a third-party course, behind the admin
# check. A crawler cannot sign in, so what it would index is the
# same "loading" shell under every address in the section.
Disallow: /skills/courses/

Sitemap: https://reiad.co.uk/sitemap.xml
