{
  "name": "reiad-app",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && TARGET=desk vite build && TARGET=studio vite build",
    "check": "tsc -b --noEmit",
    "build:desk": "tsc -b && TARGET=desk vite build",
    "build:studio": "tsc -b && TARGET=studio vite build"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
